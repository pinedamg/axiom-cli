name: developer-rg
command_pattern: "^(rg|grep)"
rules:
  - name: SearchMatch
    pattern: "^[^:]+:[0-9]+:.*"
    action: collapse
    priority: 1
  - name: FileStats
    pattern: "^[0-9]+ matches? in [0-9]+ files?.*"
    action: keep
    priority: 10
