name: docker-suite
command_pattern: "^docker\\s+(ps|container\\s+ls).*"
rules:
  - name: Header
    pattern: "^CONTAINER ID.*"
    action: collapse
    priority: 20
  - name: ContainerLine
    pattern: "^[a-f0-9]{12}\\s+.*"
    action: synthesize
    priority: 15
